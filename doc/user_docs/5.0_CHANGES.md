# Benutzer-relevante Änderungen in Foodsoft v5.0

## Artikel-Daten

Prizipielles (TODO): Alle Zahlenfelder erlauben Kommastellen. Keine Freitexte für Einheiten mehr.

### Obsolete Felder:

- Freitext *Einheit* (Einheiten sollten künftig - damit die automatische Umrechnung möglich ist - aus den bereitgestellten Auswahllisten gewählt werden. Falls tatsächlich eine benutzerdefinierte Einheit nötig ist, kann unter *Bestellbar von Lieferantin in* die Option *Benutzerdefiniert* gewählt werden, dann erscheint wieder die bisherige Texteingabe.)
- Freitext *Gebindegröße* (Diese wird künftig unter *Inhalt* abgebildet - siehe "Neue Felder")


### Neue Felder:

#### Bestellbar von Lieferantin in

In dieser Einheit werden Artikel beim Lieferanten bestellt.

##### Gebinde

Gebinde werden grundsätzlich auf Basis dieser Einheit berechnet - Beispiel:

- Für *Bestellbar von Lieferantin in* ist "Stück" ausgewählt.
- Der *Inhalt* wurde als "1 Kilogramm" definiert.
- Bei *Für Mitglieder bestellbar in* ist "100 x Gramm" ausgewählt.

-> Dann können Bestellgruppen in 100 Gramm Tranchen bestellen, der Artikel wird beim Lieferanten aber nur in vollen Gebindengrößen von 1000g bestellt. (Beispiele: 1100g -> 1000g werden bestellt, 2400g -> 2000g werden bestellt, 2400g + 600g Toleranz -> 3000g werden bestellt)

*Achtung:* Gebinde werden nur dann angewendet, wenn bei *Bestellbar von Lieferantin in* eine Stückeinheit gewählt wurde (z.B. Stück, Packung, Glas,...) und nicht eine SI-basierte Einheit (z.B. Gramm, Kilogramm, Liter...) - Beispiel:

- Für *Bestellbar von Lieferantin in* ist "Kilogramm" ausgewählt.
- Bei *Für Mitglieder bestellbar in* ist "100 x Gramm" ausgewählt.

Dann können Bestellgruppen in 100 Gramm Tranchen bestellen und es werden dabei keine Gebindegrößen bei der Bestellung beim Lieferanten berechnet. (Beispiele: 1100g -> 1100g werden bestellt, 2400g -> 2400g werden bestellt)

##### Verwendung der Einheit

Die Einheit wird an folgenden Stellen in der Foodsoft verwendet:

- Bestellung -> Herunterladen -> Fax PDF/Text/CSV

#### Inhalt

Hier können optional mehrere Zeilen hinzugefügt werden, die den Inhalt der unter *Bestellbar von Lieferantin in* gewählten Einheit definieren.
Der Inhalt der der ersten Zeile folgenden Zeilen beziehen sich immer auf die der vorhergehnden Zeile.

Beispiel Bierkiste: *Bestellbar von Lieferantin in* ist "Kiste", erste Zeile "20 x Flasche pro Kiste", zweite Zeile "0.5 Liter pro Flasche".

#### Mindestbestellmenge

Optionale Menge (bezogen auf *Bestellbar von Lieferantin in*) die ingesamt von allen Bestellgruppen einer Bestellung erreicht werden muss, damit dieser Artikel auch tatsächlich beim Lieferanten bestellt wird. (Ähnlich wie bei der Gebindegröße kann auch hier von den einzelnen Bestellgruppen eine Toleranz angegeben werden, für den Fall das die Bestellung noch unter der Mindestbestellmenge liegt.)

#### Wird abgerechnet nach

Artikeleinheit, in die an folgenden Stellen in der Foodsoft umgerechnet wird:

- Bestellung -> Sortiert nach Gruppen/Artikeln
- Bestellung -> In Empfang nehmen
- Bestellung -> Selbstbedienung
- Finanzen -> Bestellung abrechnen


Hinweis: Feld ist nur verfügbar, wenn *Bestellbar von Lieferantin in* auf andere Einheiten umgerechnet werden kann (D.h. wenn es entweder auf einer SI-Einheit wie Kilogramm basiert oder über *Inhalt* definiert wurde, worauf sie umgerechnet werden kann.)

#### Für Mitglieder bestellbar in

Gibt die Granularität pro gewählter Einheit an, mit der Bestellgruppen bestellen können.

Beispiel: 100 x Gramm bedeutet, dass Bestellgruppen z.B. "200 Gramm" bestellen können, nicht aber "240 Gramm".

Hinweis: Das Auswahlfeld für die Einheit (z.B. Gramm) ist hierbei nur verfügbar, wenn *Bestellbar von Lieferantin in* auf andere Einheiten umgerechnet werden kann. Die Menge, in der pro Bestellgruppe bestellt werden darf (Granularität), kann jedoch immer modifiziert werden.

Einheit wird an folgenden Stellen in der Foodsoft verwendet:

- Gruppenbestellung erstellen/editieren/betrachten
- Bestellung -> Herunterladen -> Gruppen/Artikel/Matrix PDF
- Bestellung -> Artikelübersicht

#### Nettopreis

Erlaubt nun zusätzlich das Wählen einer Einheit pro der der Preis gilt (Die Auswahl der Einheit dient nur zur Artikel-Administration - für den Fall, dass in den Preislisten Preise in einer anderen Einheit angegeben wird als *Bestellbar von Lieferantin in*.)

Beispiel: "0.50€ pro Dekagramm"

Hinweis: Das Auswahlfeld für die Einheit (z.B. Kiste) ist hierbei nur verfügbar, wenn *Bestellbar von Lieferantin in* auf andere Einheiten umgerechnet werden kann.

### Umstellung der Artikeldaten von Version 4.9

#### Automatische Umstellung

Die Umstellung der Artikeldaten von foodsoft Version 4.9 auf 5.0 passiert zum Teil automatisch:

*Bestellbar von Lieferantin in* wird initial für alle Artikel auf *Benutzerdefiniert* gestellt und das alte Freitext-Einheiten-Feld wird nach wie vor mit demselben Inhalt angezeigt.
War auch das nun obsolete Freitext-Feld *Gebindegröße* befüllt, wird *Inhalt* vorbefüllt mit "1 Stück" und von *Für Mitglieder bestellbar in* wird ebenfalls auf "1 Stück" gesetzt.

#### Migrations-Assistent

In der Artikelliste eines Lieferanten steht einmalig die Funktion "Alte Artikeleinheiten migrieren..." zur Verfügung. Unter dieser kann man im ersten Schritt ein Backup der Artikel als CSV herunterladen.
Im zweiten Schritt versucht das System die alten Freitext Einheiten auf die neuen exakten Einheiten abzubilden. Die automatische Erkennung kann noch bearbeitet werden, ehe man die Migration durchführt.

# Artikel-Versionen

Intern wurde die Artikel-Versionierung aktualisiert - das sollte für Benutzer:innen nur dadurch erkennbar sein, dass in 4.9 bestehende Fehler nicht mehr auftreten.

Grundsätzlich funktioniert die Artikel-Versionierung so:

Ist eine Bestellung und man editiert einen darin enthaltenen Artikel (egal, welches der Felder, Name, Preis,...) so ändern sich die Daten für die Bestellung auch noch.
Ist eine Bestellung jedoch geschlossen, so bleiben alle enthaltenen Verweise auf diesen Artikel grundsätzlich gleich ("eingefroren") wie im Moment, in dem die Bestellung geschlossen wurde. (Bearbeiten des Artikels in der Artikelliste dieser Lieferantin hat keinen Effekt.)

# Lieferanten-Synchronisation
# Umrechnungsfelder
# Selbstabholung

# TODO - remove this:

Gruppenbestellung erstellen/editieren/betrachten: *Für Mitglieder bestellbar in* (`group_order_unit`)
Bestellung -> Herunterladen -> Gruppen/Artikel PDF: *Wird abgerechnet nach* (`billing_unit`)
Bestellung -> Herunterladen -> Fax PDF/Text/CSV: *Bestellbar von Lieferantin in* (`supplier_order_unit`)
Bestellung -> Artikelübersicht: *Für Mitglieder bestellbar in* (`group_order_unit`)
Bestellung -> Sortiert nach Gruppen/Artikeln: *Wird abgerechnet nach* (`billing_unit`)
Bestellung -> In Empfang nehmen: *Wird abgerechnet nach* (`billing_unit`)
Selbstbedienung: *Wird abgerechnet nach* (`billing_unit`)
Bestellung abrechnen: *Wird abgerechnet nach* (`billing_unit`)

*Bestellbar von Lieferantin in* (`supplier_order_unit`)

- Bestellung -> Herunterladen -> Fax PDF/Text/CSV

*Wird abgerechnet nach* (`billing_unit`)

- Bestellung -> Sortiert nach Gruppen/Artikeln
- Bestellung -> In Empfang nehmen
- Bestellung -> Selbstbedienung
- Finanzen -> Bestellung abrechnen

*Für Mitglieder bestellbar in* (`group_order_unit`)

- Bestellung -> Herunterladen -> Gruppen/Artikel/Matrix PDF
- Gruppenbestellung erstellen/editieren/betrachten
- Bestellung -> Artikelübersicht

